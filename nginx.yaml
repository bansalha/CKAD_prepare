apiVersion: v1
kind: Pod
metadata:
  creationTimestamp: null
  labels:
    run: nginx
  name: nginx
spec:
  volumes:
    - name: vol
      configMap:
        name: cmvolume
  containers:
  - image: nginx
    name: nginx
    volumeMounts:
    - name: vol
      mountPath: /etc/lala
    resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Always
status: {}
